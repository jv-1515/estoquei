spring.application.name=estoquei

# Configurações do banco de dados MySQL
# Certifique-se de que o MySQL está rodando na porta 3306 e que o banco de dados estoquei foi criado
# spring.datasource.url=jdbc:mysql://localhost:3306/estoquei?useSSL=false&serverTimezone=UTC

# Apenas para desenvolvimento, não recomendado para produção
spring.datasource.url=jdbc:mysql://localhost:3306/estoquei?useSSL=false&allowPublicKeyRetrieval=true

# Configurações do DataSource
spring.datasource.username=root
spring.datasource.password=cannarozzo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do DataSource
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configurações do Firebase
firebase.bucket-name=stokmais.firebasestorage.app
firebase.service-account-file=classpath:stokmais-firebase-adminsdk-fbsvc-4754f9aa57.json

# Configurações do Spring Security
spring.servlet.multipart.max-file-size=7MB
spring.servlet.multipart.max-request-size=7MB